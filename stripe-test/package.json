{
  "name": "quantumguard-stripe-test",
  "version": "1.0.0",
  "description": "Isolated Stripe Checkout test slice for QuantumGuard (webhook is the only confirmation boundary)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "stripe": "^16.12.0"
  }
}
